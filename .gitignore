# IDE / local tools
.claude
.continue
.DS_Store
.idea
.vscode
.fleet
framer-designs

# OS / editor temp files
*.swp
*.swo
*~

# Environment files
.env
.env.backup
.env.production

# Dependency directories (if any at root)
/node_modules

# Build / deploy artifacts generated by deploy.sh
/deploy
family-tree-deploy.tar.gz

# PHP / Laravel artifacts that might end up at root
/vendor
.phpunit.result.cache

# Root-level docs and helper scripts (keep repo focused on source code)
*.md
*.sh
server-setup.php
query.php
query-db.sh
